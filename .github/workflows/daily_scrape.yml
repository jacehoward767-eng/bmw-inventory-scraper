name: Daily Inventory Scrape

on:
  schedule:
    # 12:00 UTC = 7:00 AM Central Daylight Time
    - cron: '0 12 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  scrape:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Google Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl unzip
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo gpg --dearmor -o /usr/share/keyrings/googlechrome-linux-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/googlechrome-linux-keyring.gpg] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Inventory Scraper
        run: python maps.py

      - name: Stage Artifacts for Git Commit and GitHub Pages
        run: |
          mkdir -p _site
          cp inventory.json _site/index.json
          
          # Find the freshest timestamped Excel report and stage it under a fixed name
          LATEST_EXCEL=$(ls -t BMW_Inventory_Reports/BMW_Inventory_*.xlsx | head -n 1)
          cp "$LATEST_EXCEL" latest_inventory.xlsx
          cp "$LATEST_EXCEL" _site/latest_inventory.xlsx

      - name: Commit and Push Reports to Repository
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Automated daily inventory snapshot'
          file_pattern: 'BMW_Inventory_Reports/*.xlsx inventory.json latest_inventory.xlsx'

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
